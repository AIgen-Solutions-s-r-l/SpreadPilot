# MongoDB Configuration
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=password
MONGO_DB_NAME=spreadpilot_admin

# Admin API Configuration
ADMIN_USERNAME=admin
# Default password is 'admin123' - change this in production!
ADMIN_PASSWORD_HASH=$2b$12$Vpj/uoizndG.JmoQg09vIO./kQioWRFnEESlnVBhz9Pbck0EO1CK2
# JWT_SECRET — REQUIRED. Minimum 64 characters. Known-weak placeholders are rejected at startup.
# Generate a real secret with: openssl rand -hex 32
# The value below is intentionally obvious — DO NOT reuse in any shared or staging environment.
JWT_SECRET=DEV_ONLY_NOT_A_SECRET_regenerate_with_openssl_rand_hex_32_aaaaaaaa
CORS_ORIGINS=http://localhost:3000,http://localhost:5173