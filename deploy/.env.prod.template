# deploy/.env.prod.template
# Template for Production Environment Variables
# Variables marked with "(Secret Manager)" should be stored in GCP Secret Manager
# with the name format: <secret-name>-prod:latest (e.g., firebase-sa-prod:latest)

# --- General ---
ENV=prod
GCP_PROJECT=  # Production GCP project ID

# --- Admin API ---
# FIREBASE_SERVICE_ACCOUNT= (Secret Manager: firebase-sa-prod:latest)
ADMIN_API_BASE_URL=  # Production Admin API Cloud Run URL

# --- Trading Bot ---
# IBKR_ACCOUNT= (Secret Manager: ibkr-account-prod:latest)
# IBKR_PASSWORD= (Secret Manager: ibkr-password-prod:latest)
# TELEGRAM_BOT_TOKEN= (Secret Manager: telegram-token-prod:latest)
# TELEGRAM_CHAT_ID=  # Production Telegram chat ID (or use Secret Manager)

# --- Report Worker ---
# SENDGRID_API_KEY= (Secret Manager: sendgrid-key-prod:latest)
# REPORT_RECIPIENT_EMAIL=  # Production report recipient email
# MinIO/S3 Configuration (Optional - for report storage)
# MINIO_ENDPOINT_URL=  # Production MinIO/S3 endpoint URL
# MINIO_ACCESS_KEY= (Secret Manager: minio-access-key-prod:latest)
# MINIO_SECRET_KEY= (Secret Manager: minio-secret-key-prod:latest)
# MINIO_BUCKET_NAME=spreadpilot-reports
# MINIO_REGION=us-east-1
# MINIO_SECURE=true

# --- Alert Router ---
# (Uses TELEGRAM_BOT_TOKEN from Trading Bot section, ensure it's the prod token)
ALERT_ROUTER_TELEGRAM_CHAT_ID=  # Production alert Telegram chat ID

# --- Watchdog ---
# (Uses TELEGRAM_BOT_TOKEN from Trading Bot section, ensure it's the prod token)
WATCHDOG_TELEGRAM_CHAT_ID=  # Production watchdog Telegram chat ID

# --- Frontend ---
REACT_APP_API_URL=  # Production Admin API URL (must match ADMIN_API_BASE_URL)

# --- Add other service-specific variables as needed ---
# MongoDB Credentials
MONGO_INITDB_ROOT_USERNAME=  # Production MongoDB root username
MONGO_INITDB_ROOT_PASSWORD=  # Production MongoDB root password (use Secret Manager)