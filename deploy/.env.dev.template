# deploy/.env.dev.template
# Template for Development Environment Variables
# Variables marked with "(Vault)" should be stored in HashiCorp Vault
# with the path format: secret/<secret-name> (e.g., secret/firebase-sa)

# --- General ---
ENV=dev
GCP_PROJECT=  # Development GCP project ID

# --- Admin API ---
# ADMIN_USERNAME= (Vault: secret/admin-credentials)
# ADMIN_PASSWORD_HASH= (Vault: secret/admin-credentials)
# JWT_SECRET — REQUIRED when VAULT_ENABLED=false (Vault path: secret/jwt-secret otherwise).
# Minimum 64 characters. Known-weak placeholders are rejected by admin-api at startup.
# Generate a real secret with: openssl rand -hex 32
# The value below is an obvious DEV ONLY placeholder — never reuse in shared/staging/prod.
JWT_SECRET=DEV_ONLY_NOT_A_SECRET_regenerate_with_openssl_rand_hex_32_aaaaaaaa
# MANUAL_OPERATION_PIN= (Vault: secret/manual-pin)
ADMIN_API_BASE_URL=  # Development Admin API Cloud Run URL

# --- Trading Bot ---
# IBKR credentials stored per follower in Vault: secret/ibkr/<follower_id>
# TELEGRAM_BOT_TOKEN= (Vault: secret/telegram-token)
# TELEGRAM_CHAT_ID=  # Development Telegram chat ID (or use Vault)

# --- Report Worker ---
# SENDGRID_API_KEY= (Vault: secret/sendgrid-key)
# REPORT_RECIPIENT_EMAIL=  # Development report recipient email
# MinIO/S3 Configuration (Optional - for report storage)
# MINIO_ENDPOINT_URL=  # Development MinIO/S3 endpoint URL
# MINIO_ACCESS_KEY= (Vault: secret/minio-access-key)
# MINIO_SECRET_KEY= (Vault: secret/minio-secret-key)
# MINIO_BUCKET_NAME=spreadpilot-reports-dev
# MINIO_REGION=us-east-1
# MINIO_SECURE=true

# --- Alert Router ---
# (Uses TELEGRAM_BOT_TOKEN from Trading Bot section)
ALERT_ROUTER_TELEGRAM_CHAT_ID=  # Development alert Telegram chat ID

# --- Watchdog ---
# (Uses TELEGRAM_BOT_TOKEN from Trading Bot section)
WATCHDOG_TELEGRAM_CHAT_ID=  # Development watchdog Telegram chat ID

# --- Frontend ---
REACT_APP_API_URL=  # Development Admin API URL (must match ADMIN_API_BASE_URL)

# --- Add other service-specific variables as needed ---
# MongoDB Credentials
MONGO_INITDB_ROOT_USERNAME=  # Development MongoDB root username
MONGO_INITDB_ROOT_PASSWORD=  # Development MongoDB root password