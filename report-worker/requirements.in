# Base dependencies
flask
python-dotenv
google-cloud-firestore
google-cloud-pubsub
google-cloud-logging

# Core library (assuming it's installed locally during build or from a private repo)
# If spreadpilot-core is published, add its package name here.
# For local development/build:
-e ../spreadpilot-core

# Reporting specific
# pandas might be needed for P&L aggregation if not handled by core utils
# openpyxl is needed by spreadpilot_core.utils.excel
openpyxl
# reportlab is needed by spreadpilot_core.utils.pdf
reportlab