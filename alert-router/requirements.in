# Base dependencies
fastapi
uvicorn[standard]
pydantic-settings
google-cloud-pubsub

# Core library (assuming it's installed locally in editable mode or from a private registry)
# If installed locally: pip install -e ../spreadpilot-core
# spreadpilot-core

# For local development/testing if needed
# httpx